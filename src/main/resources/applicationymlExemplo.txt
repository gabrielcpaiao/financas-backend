server:
  port: 8080

spring:
  application:
    name: financas-backend
  datasource:
    # url: jdbc:mysql://localhost:3306/SEUBANCO?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
    url: jdbc:mysql://localhost:3306/SEUBANCO?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: SUASENHA
  jpa:
    hibernate:
      ddl-auto: validate   # o schema é gerenciado pelo Flyway, nunca pelo Hibernate
    show-sql: false
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  jwt:
    secret: ${JWT_SECRET:troque-este-segredo-por-um-valor-longo-e-aleatorio}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}   # 24h
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
